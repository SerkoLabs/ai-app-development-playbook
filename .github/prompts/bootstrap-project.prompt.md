Read `AGENTS.md` and start the lifecycle for the product idea I provide. Determine the current state from repository evidence, complete planning stages in the required order, enforce every planning gate, update `docs/PROJECT_STATUS.md`, and do not write application code before Stage 07 passes.

Product idea:
${input:idea:Describe the app idea}
